import Animated from "./Animated.svelte";
import Temporary from "./Temporary.svelte";

const Edge = {
	Temporary,
	Animated,
};

export { Edge, Temporary as EdgeTemporary, Animated as EdgeAnimated };
